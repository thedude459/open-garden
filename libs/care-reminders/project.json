{
  "name": "care-reminders",
  "$schema": "../../node_modules/nx/schemas/project.json",
  "sourceRoot": "libs/care-reminders/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:care", "layer:domain"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/care-reminders --passWithNoTests --config vitest.config.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint \"libs/care-reminders/**/*.ts\"",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
