{
  "name": "plant-favorites",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/plant-favorites/src",
  "projectType": "library",
  "tags": [
    "type:lib",
    "scope:plant",
    "layer:domain"
  ],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/plant-favorites --passWithNoTests --config vitest.config.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint \"libs/plant-favorites/**/*.ts\"",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
