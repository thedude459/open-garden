{
  "name": "catalog-pipeline",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/catalog-pipeline/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:pipeline", "layer:domain"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/catalog-pipeline --passWithNoTests --config vitest.config.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint \"libs/catalog-pipeline/**/*.ts\"",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
